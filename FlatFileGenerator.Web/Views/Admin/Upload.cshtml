@{
    ViewData["Title"] = "Upload";
}

<h2>Upload File</h2>
<p>&nbsp;</p>

<div class="row">
    <div class="col-md-6">
        <section>
            <h3>
                Uploading a receiptDetail file
            </h3>
            <form asp-action="Uploadfile" method="post" enctype="multipart/form-data" class="form-horizontal" role="form">
                <p>Select file:</p>
                <div class="form-group">
                    <div class="col-md-10">
                        <input type="file" class="btn btn-secondary" name="file" />
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-10">
                        <select id="type" name="type" class="form-select">
                            <option Value="1">JSON</option>
                            <option Value="2">IpStandard</option>
                            <option Value="3">CSV</option>
                            <option Value="4">Excel</option>
                            <option Value="10">Nets Informationsservice</option>
                        </select>
                        <p></p>
                        <button type="submit" class="btn btn-primary">OK</button>
                    </div>
                </div>
            </form>
            <p></p>
            &nbsp;
        </section>
    </div>
</div>
